#include <stdio.h>

/**
 * main - your function goes in here
 * @argc: number of arguments
 * @argv: arguments passed in the function
 *
 * Return: Always 0 (Success)
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", *argv);

	return (0);
}
