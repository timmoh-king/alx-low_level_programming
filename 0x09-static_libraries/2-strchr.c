#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strchr - print string
 * @s: the string array
 * @c: the character to be checked
 *
 * Return: strchr function
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
