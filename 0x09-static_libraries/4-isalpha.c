#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isalpha - return only alphabet values
 * @c: the character to be checked
 *
 * Return: the alpabetical value
 */
int _isalpha(int c)
{
	return (isalpha(c));
}
