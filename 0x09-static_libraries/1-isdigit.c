#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _isdigit - return digits
 * @c: the integer to be checked
 *
 * Return: integer character
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
