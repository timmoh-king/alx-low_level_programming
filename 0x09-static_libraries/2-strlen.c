#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - print the lenth of a string
 * @s: the array string
 *
 * Return: the length of string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
